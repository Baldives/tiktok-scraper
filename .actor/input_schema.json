{
  "title": "TikTok Scraper Actor",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "type": {
      "title": "Scrape Type",
      "description": "Choose what to scrape: user, hashtag, trend, music, video",
      "type": "string",
      "enum": ["user", "hashtag", "trend", "music", "video"],
      "editor": "select",
      "default": "hashtag"
    },
    "query": {
      "title": "Query",
      "description": "The username, hashtag, music ID, or video URL depending on the type",
      "type": "string",
      "editor": "textfield"
    },
    "number": {
      "title": "Number of posts",
      "description": "Number of posts to scrape. 0 means all posts",
      "type": "number",
      "editor": "number",
      "default": 10
    },
    "filetype": {
      "title": "Output file type",
      "description": "Type of the output file",
      "type": "string",
      "enum": ["json", "csv", "all"],
      "editor": "select",
      "default": "json"
    },
    "filename": {
      "title": "Output filename",
      "description": "Custom filename for saved output",
      "type": "string",
      "editor": "textfield",
      "default": ""
    },
    "sessionFile": {
      "title": "Session file",
      "description": "Path to the file with TikTok cookies",
      "type": "string",
      "editor": "fileupload",
      "default": "apify/session.txt"
    },
    "useApifyProxy": {
      "title": "Use Apify Residential Proxy",
      "description": "Enable Apify proxy to avoid blocking",
      "type": "boolean",
      "editor": "checkbox",
      "default": true
    },
    "apifyProxyGroups": {
      "title": "Proxy Groups",
      "description": "Which Apify proxy groups to use (comma-separated)",
      "type": "array",
      "editor": "textfield",
      "default": ["RESIDENTIAL"]
    },
    "apifyProxySession": {
      "title": "Proxy Session ID",
      "description": "Optional custom session ID for proxy",
      "type": "string",
      "editor": "textfield",
      "default": "session-1"
    }
  },
  "required": ["type", "query"]
}

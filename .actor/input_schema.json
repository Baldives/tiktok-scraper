{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TikTok Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Search Query",
      "description": "TikTok username, hashtag, or music ID to scrape",
      "type": "string",
      "editor": "textfield"
    },
    "type": {
      "title": "Query Type",
      "description": "Specify what type of query this is",
      "type": "string",
      "editor": "select",
      "enum": ["user", "hashtag", "trend", "music", "video"],
      "default": "user"
    },
    "number": {
      "title": "Number of Posts",
      "description": "Number of posts to scrape (0 = all)",
      "type": "integer",
      "editor": "number",
      "default": 0
    },
    "sessionFile": {
      "title": "Session File Path",
      "description": "Path to your session.txt file containing TikTok cookies",
      "type": "string",
      "editor": "fileupload",
      "default": "apify/session.txt"
    },
    "apifyProxyGroups": {
      "title": "Proxy Groups",
      "description": "Which Apify proxy groups to use",
      "type": "string",
      "editor": "select",
      "enum": ["RESIDENTIAL", "DATACENTER", "PUBLIC"],
      "default": "RESIDENTIAL"
    },
    "apifyProxySession": {
      "title": "Proxy Session ID",
      "description": "Optional custom session ID for the proxy",
      "type": "string",
      "editor": "textfield",
      "default": "session-1"
    },
    "download": {
      "title": "Download Videos",
      "description": "Whether to download videos (you said no, so default false)",
      "type": "boolean",
      "editor": "hidden",
      "default": false
    },
    "filetype": {
      "title": "Output File Type",
      "description": "Format for scraped data",
      "type": "string",
      "editor": "select",
      "enum": ["json", "csv", "all"],
      "default": "json"
    }
  },
  "required": ["query", "type"]
}

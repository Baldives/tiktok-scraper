{
  "schemaVersion": 1,
  "title": "TikTok Scraper",
  "type": "object",
  "properties": {
    "query": {
      "title": "Query",
      "description": "Username, hashtag, video URL, or music ID to scrape",
      "type": "string",
      "editor": "textfield"
    },
    "type": {
      "title": "Scrape type",
      "description": "Choose what type of feed to scrape",
      "type": "string",
      "editor": "select",
      "options": ["user", "hashtag", "trend", "music", "video"],
      "enumSuggestedValues": ["user", "hashtag", "trend", "music", "video"]
    },
    "filetype": {
      "title": "Output file type",
      "description": "Choose the format of the output file",
      "type": "string",
      "editor": "select",
      "options": ["json", "csv", "all"],
      "enumSuggestedValues": ["json", "csv", "all"]
    },
    "sessionFile": {
      "title": "Session file",
      "description": "Path to session.txt with your TikTok cookies",
      "type": "string",
      "editor": "fileupload"
    },
    "number": {
      "title": "Number of posts",
      "description": "Number of posts to scrape (0 = all)",
      "type": "integer",
      "default": 0,
      "editor": "textfield"
    },
    "download": {
      "title": "Download videos",
      "description": "Download video files along with metadata",
      "type": "boolean",
      "default": false
    },
    "filepath": {
      "title": "Output folder",
      "description": "Where to save downloaded videos or data",
      "type": "string",
      "editor": "textfield"
    }
  },
  "required": ["query", "type"]
}
